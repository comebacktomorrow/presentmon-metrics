global:
  scrape_interval: 5s
rule_files:
  - /etc/prometheus/rules.yml
scrape_configs:
  - job_name: kiosk-presentmon
    static_configs:
      - targets: ['host.docker.internal:9110']
        labels:
          instance: dev-box-chrome
